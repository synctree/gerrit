Vagrant.configure("2") do |c|
  c.vm.box = "opscode-ubuntu"
  c.vm.box_url = "https://opscode-vm-bento.s3.amazonaws.com/vagrant/virtualbox/opscode_ubuntu_chef-provisionerless.box"
  c.vm.hostname = "default-ubuntu.vagrantup.com"
  c.vm.synced_folder ".", "/vagrant", disabled: true
  c.vm.provider :virtualbox do |p|
    p.customize ["modifyvm", :id, "--memory", "256"]
  end
end
